# Optional. Only needed if you want exact, real-time CurseForge numbers via
# the official API instead of the free cfwidget.com proxy (which can lag by
# up to ~15 minutes but needs no key). Get a key at https://console.curseforge.com/
# and pass a numeric mod id (?id=) to /api/badge/curseforge to use it.
CURSEFORGE_API_KEY=
